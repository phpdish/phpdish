thread:
    slug:
        unique: 该slug已经被占用
        not_blank: Slug不得为空
        regex: 只能包含字母数字或者横线
    name:
        not_blank: 名称不得为空
        min_length: 名称至少{{ limit }}字符长度
          : 名称最多{{ limit }}字符长度
    description:
        max_length: 描述最多{{ limit }}字符长度

topic:
    title:
        not_blank: 标题不得为空
        min_length: 标题至少{{ limit }}字符长度
        max_length: 标题最多{{ limit }}字符长度
    original_body:
        not_blank: 内容不得为空
        min_length: 内容至少{{ limit }}字符长度
    threads:
        not_blank: 节点不得为空